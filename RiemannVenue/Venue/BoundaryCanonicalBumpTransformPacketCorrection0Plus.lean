import RiemannVenue.Venue.BoundaryCanonicalBumpTransformPacketGrid

/-!
# Canonical-bump transform packet: Correction0Plus

Generated by `scripts/generate_canonical_bump_transform_packets.py`. Do not edit by hand.
All packet data are exact rationals; Lean remains proof authority.
-/

namespace RiemannVenue.Venue

open MeasureTheory Set
open scoped BigOperators Interval

noncomputable section

def canonicalBumpCorrection0PlusReal : ℚ := (731583688770142887 / 7600000000000000 : ℚ)

theorem canonicalBumpCorrection0PlusReal_bound : |canonicalBumpCorrection0PlusReal| ≤ 103 := by
  norm_num [canonicalBumpCorrection0PlusReal]

theorem canonicalBumpCorrection0Plus_kernel_trig (i : Fin 128) :
    |(((canonicalBumpCorrection0PlusReal * canonicalBumpPacketMidpoint i : ℚ) : ℝ) / 16)| / 33 ≤
      (1 : ℝ) / 2 := by
  have hre : |(canonicalBumpCorrection0PlusReal : ℝ)| ≤ 103 := by
    exact_mod_cast canonicalBumpCorrection0PlusReal_bound
  push_cast
  rw [abs_div, abs_mul]
  norm_num
  calc
    |(canonicalBumpCorrection0PlusReal : ℝ)| * |(canonicalBumpPacketMidpoint i : ℝ)| / 16 / 33 ≤
        103 * 1 / 16 / 33 := by
      gcongr
      exact canonicalBumpPacket_midpoint_abs i
    _ ≤ (1 : ℝ) / 2 := by norm_num

noncomputable def canonicalBumpCorrection0PlusCell (i : Fin 128) :
    ComplexIntegralCellCertificate
      (computedTransformRawIntegrand canonicalSmoothBump
        ((canonicalBumpCorrection0PlusReal : ℝ) +
          (canonicalBumpPacketImaginaryPart : ℝ) * Complex.I))
      (equalCellPoint (-1) 1 128 i)
      (equalCellPoint (-1) 1 128 (i + 1)) :=
  canonicalBumpPacketTransportCell
    (canonicalBumpTaylorCellAt
    canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
    (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
    canonicalBumpPacket_radius_nonneg
    canonicalBumpCorrection0PlusReal_bound canonicalBumpPacket_imaginary_bound
    (canonicalBumpPacket_kernel_exp i)
    (canonicalBumpCorrection0Plus_kernel_trig i)
    (canonicalBumpPacket_bump_lower i)
    (canonicalBumpPacket_bump_upper i)
    (canonicalBumpPacket_window i))
    (canonicalBumpPacket_left_geometry_real i)
    (canonicalBumpPacket_right_geometry_real i)

noncomputable def canonicalBumpCorrection0PlusPacket :
    EqualCellComplexTaylorCertificate
      (computedTransformRawIntegrand canonicalSmoothBump
        ((canonicalBumpCorrection0PlusReal : ℝ) +
          (canonicalBumpPacketImaginaryPart : ℝ) * Complex.I))
      (-1) 1 128 12 where
  cells_pos := by norm_num
  ordered := by norm_num
  integrable := fun _ =>
    (canonicalBumpRawIntegrand_contDiff _).continuous.intervalIntegrable _ _
  cell := canonicalBumpCorrection0PlusCell

def canonicalBumpCorrection0PlusRawCenterQ : ℚ × ℚ :=
  (∑ i : Fin 128, (canonicalBumpCellCenterQ
      canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).re.center,
    ∑ i : Fin 128, (canonicalBumpCellCenterQ
      canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).im.center)

def canonicalBumpCorrection0PlusRawErrorQ : ℚ :=
  ∑ i : Fin 128, canonicalBumpCellErrorQ
    canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
    (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius

theorem canonicalBumpCorrection0PlusCell_center (i : Fin 128) :
    (canonicalBumpCorrection0PlusCell i).center =
      ((canonicalBumpCellCenterQ
        canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).re.center : ℝ) +
      ((canonicalBumpCellCenterQ
        canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).im.center : ℝ) *
        Complex.I := by
  rw [canonicalBumpCorrection0PlusCell, canonicalBumpPacketTransportCell_center]
  exact canonicalBumpTaylorCellAt_center
      canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
      canonicalBumpPacket_radius_nonneg
      canonicalBumpCorrection0PlusReal_bound canonicalBumpPacket_imaginary_bound
      (canonicalBumpPacket_kernel_exp i)
      (canonicalBumpCorrection0Plus_kernel_trig i)
      (canonicalBumpPacket_bump_lower i)
      (canonicalBumpPacket_bump_upper i)
      (canonicalBumpPacket_window i)

theorem canonicalBumpCorrection0PlusCell_error (i : Fin 128) :
    (canonicalBumpCorrection0PlusCell i).error =
      (canonicalBumpCellErrorQ
        canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius : ℝ) := by
  rw [canonicalBumpCorrection0PlusCell, canonicalBumpPacketTransportCell_error]
  exact canonicalBumpTaylorCellAt_error
      canonicalBumpCorrection0PlusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
      canonicalBumpPacket_radius_nonneg
      canonicalBumpCorrection0PlusReal_bound canonicalBumpPacket_imaginary_bound
      (canonicalBumpPacket_kernel_exp i)
      (canonicalBumpCorrection0Plus_kernel_trig i)
      (canonicalBumpPacket_bump_lower i)
      (canonicalBumpPacket_bump_upper i)
      (canonicalBumpPacket_window i)

theorem canonicalBumpCorrection0PlusPacket_center :
    canonicalBumpCorrection0PlusPacket.center =
      ((canonicalBumpCorrection0PlusRawCenterQ.1 : ℚ) : ℝ) +
        ((canonicalBumpCorrection0PlusRawCenterQ.2 : ℚ) : ℝ) * Complex.I := by
  rw [EqualCellComplexTaylorCertificate.center]
  unfold canonicalBumpCorrection0PlusPacket canonicalBumpCorrection0PlusRawCenterQ
  simp_rw [show ∀ i : Fin 128,
    (canonicalBumpCorrection0PlusCell i).center = _ from canonicalBumpCorrection0PlusCell_center]
  push_cast
  rw [Finset.sum_add_distrib, Finset.sum_mul]

theorem canonicalBumpCorrection0PlusPacket_error :
    canonicalBumpCorrection0PlusPacket.error = (canonicalBumpCorrection0PlusRawErrorQ : ℝ) := by
  rw [EqualCellComplexTaylorCertificate.error]
  unfold canonicalBumpCorrection0PlusPacket canonicalBumpCorrection0PlusRawErrorQ
  simp_rw [canonicalBumpCorrection0PlusCell_error]
  push_cast
  rfl

end

end RiemannVenue.Venue
