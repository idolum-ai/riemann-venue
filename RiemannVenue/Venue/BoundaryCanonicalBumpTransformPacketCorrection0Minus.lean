import RiemannVenue.Venue.BoundaryCanonicalBumpTransformPacketGrid

/-!
# Canonical-bump transform packet: Correction0Minus

Generated by `scripts/generate_canonical_bump_transform_packets.py`. Do not edit by hand.
All packet data are exact rationals; Lean remains proof authority.
-/

namespace RiemannVenue.Venue

open MeasureTheory Set
open scoped BigOperators Interval

noncomputable section

def canonicalBumpCorrection0MinusReal : ℚ := (20383688770142887 / 7600000000000000 : ℚ)

theorem canonicalBumpCorrection0MinusReal_bound : |canonicalBumpCorrection0MinusReal| ≤ 103 := by
  norm_num [canonicalBumpCorrection0MinusReal]

theorem canonicalBumpCorrection0Minus_kernel_trig (i : Fin 128) :
    |(((canonicalBumpCorrection0MinusReal * canonicalBumpPacketMidpoint i : ℚ) : ℝ) / 16)| / 33 ≤
      (1 : ℝ) / 2 := by
  have hre : |(canonicalBumpCorrection0MinusReal : ℝ)| ≤ 103 := by
    exact_mod_cast canonicalBumpCorrection0MinusReal_bound
  push_cast
  rw [abs_div, abs_mul]
  norm_num
  calc
    |(canonicalBumpCorrection0MinusReal : ℝ)| * |(canonicalBumpPacketMidpoint i : ℝ)| / 16 / 33 ≤
        103 * 1 / 16 / 33 := by
      gcongr
      exact canonicalBumpPacket_midpoint_abs i
    _ ≤ (1 : ℝ) / 2 := by norm_num

noncomputable def canonicalBumpCorrection0MinusCell (i : Fin 128) :
    ComplexIntegralCellCertificate
      (computedTransformRawIntegrand canonicalSmoothBump
        ((canonicalBumpCorrection0MinusReal : ℝ) +
          (canonicalBumpPacketImaginaryPart : ℝ) * Complex.I))
      (equalCellPoint (-1) 1 128 i)
      (equalCellPoint (-1) 1 128 (i + 1)) :=
  canonicalBumpPacketTransportCell
    (canonicalBumpTaylorCellAt
    canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
    (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
    canonicalBumpPacket_radius_nonneg
    canonicalBumpCorrection0MinusReal_bound canonicalBumpPacket_imaginary_bound
    (canonicalBumpPacket_kernel_exp i)
    (canonicalBumpCorrection0Minus_kernel_trig i)
    (canonicalBumpPacket_bump_lower i)
    (canonicalBumpPacket_bump_upper i)
    (canonicalBumpPacket_window i))
    (canonicalBumpPacket_left_geometry_real i)
    (canonicalBumpPacket_right_geometry_real i)

noncomputable def canonicalBumpCorrection0MinusPacket :
    EqualCellComplexTaylorCertificate
      (computedTransformRawIntegrand canonicalSmoothBump
        ((canonicalBumpCorrection0MinusReal : ℝ) +
          (canonicalBumpPacketImaginaryPart : ℝ) * Complex.I))
      (-1) 1 128 12 where
  cells_pos := by norm_num
  ordered := by norm_num
  integrable := fun _ =>
    (canonicalBumpRawIntegrand_contDiff _).continuous.intervalIntegrable _ _
  cell := canonicalBumpCorrection0MinusCell

def canonicalBumpCorrection0MinusRawCenterQ : ℚ × ℚ :=
  (∑ i : Fin 128, (canonicalBumpCellCenterQ
      canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).re.center,
    ∑ i : Fin 128, (canonicalBumpCellCenterQ
      canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).im.center)

def canonicalBumpCorrection0MinusRawErrorQ : ℚ :=
  ∑ i : Fin 128, canonicalBumpCellErrorQ
    canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
    (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius

theorem canonicalBumpCorrection0MinusCell_center (i : Fin 128) :
    (canonicalBumpCorrection0MinusCell i).center =
      ((canonicalBumpCellCenterQ
        canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).re.center : ℝ) +
      ((canonicalBumpCellCenterQ
        canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).im.center : ℝ) *
        Complex.I := by
  rw [canonicalBumpCorrection0MinusCell, canonicalBumpPacketTransportCell_center]
  exact canonicalBumpTaylorCellAt_center
      canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
      canonicalBumpPacket_radius_nonneg
      canonicalBumpCorrection0MinusReal_bound canonicalBumpPacket_imaginary_bound
      (canonicalBumpPacket_kernel_exp i)
      (canonicalBumpCorrection0Minus_kernel_trig i)
      (canonicalBumpPacket_bump_lower i)
      (canonicalBumpPacket_bump_upper i)
      (canonicalBumpPacket_window i)

theorem canonicalBumpCorrection0MinusCell_error (i : Fin 128) :
    (canonicalBumpCorrection0MinusCell i).error =
      (canonicalBumpCellErrorQ
        canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius : ℝ) := by
  rw [canonicalBumpCorrection0MinusCell, canonicalBumpPacketTransportCell_error]
  exact canonicalBumpTaylorCellAt_error
      canonicalBumpCorrection0MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
      canonicalBumpPacket_radius_nonneg
      canonicalBumpCorrection0MinusReal_bound canonicalBumpPacket_imaginary_bound
      (canonicalBumpPacket_kernel_exp i)
      (canonicalBumpCorrection0Minus_kernel_trig i)
      (canonicalBumpPacket_bump_lower i)
      (canonicalBumpPacket_bump_upper i)
      (canonicalBumpPacket_window i)

theorem canonicalBumpCorrection0MinusPacket_center :
    canonicalBumpCorrection0MinusPacket.center =
      ((canonicalBumpCorrection0MinusRawCenterQ.1 : ℚ) : ℝ) +
        ((canonicalBumpCorrection0MinusRawCenterQ.2 : ℚ) : ℝ) * Complex.I := by
  rw [EqualCellComplexTaylorCertificate.center]
  unfold canonicalBumpCorrection0MinusPacket canonicalBumpCorrection0MinusRawCenterQ
  simp_rw [show ∀ i : Fin 128,
    (canonicalBumpCorrection0MinusCell i).center = _ from canonicalBumpCorrection0MinusCell_center]
  push_cast
  rw [Finset.sum_add_distrib, Finset.sum_mul]

theorem canonicalBumpCorrection0MinusPacket_error :
    canonicalBumpCorrection0MinusPacket.error = (canonicalBumpCorrection0MinusRawErrorQ : ℝ) := by
  rw [EqualCellComplexTaylorCertificate.error]
  unfold canonicalBumpCorrection0MinusPacket canonicalBumpCorrection0MinusRawErrorQ
  simp_rw [canonicalBumpCorrection0MinusCell_error]
  push_cast
  rfl

end

end RiemannVenue.Venue
