import RiemannVenue.Venue.BoundaryCanonicalBumpTransformPacketGrid

/-!
# Canonical-bump transform packet: Correction1Minus

Generated by `scripts/generate_canonical_bump_transform_packets.py`. Do not edit by hand.
All packet data are exact rationals; Lean remains proof authority.
-/

namespace RiemannVenue.Venue

open MeasureTheory Set
open scoped BigOperators Interval

noncomputable section

def canonicalBumpCorrection1MinusReal : ℚ := (-21 / 8 : ℚ)

theorem canonicalBumpCorrection1MinusReal_bound : |canonicalBumpCorrection1MinusReal| ≤ 103 := by
  norm_num [canonicalBumpCorrection1MinusReal]

theorem canonicalBumpCorrection1Minus_kernel_trig (i : Fin 128) :
    |(((canonicalBumpCorrection1MinusReal * canonicalBumpPacketMidpoint i : ℚ) : ℝ) / 16)| / 33 ≤
      (1 : ℝ) / 2 := by
  have hre : |(canonicalBumpCorrection1MinusReal : ℝ)| ≤ 103 := by
    exact_mod_cast canonicalBumpCorrection1MinusReal_bound
  push_cast
  rw [abs_div, abs_mul]
  norm_num
  calc
    |(canonicalBumpCorrection1MinusReal : ℝ)| * |(canonicalBumpPacketMidpoint i : ℝ)| / 16 / 33 ≤
        103 * 1 / 16 / 33 := by
      gcongr
      exact canonicalBumpPacket_midpoint_abs i
    _ ≤ (1 : ℝ) / 2 := by norm_num

noncomputable def canonicalBumpCorrection1MinusCell (i : Fin 128) :
    ComplexIntegralCellCertificate
      (computedTransformRawIntegrand canonicalSmoothBump
        ((canonicalBumpCorrection1MinusReal : ℝ) +
          (canonicalBumpPacketImaginaryPart : ℝ) * Complex.I))
      (equalCellPoint (-1) 1 128 i)
      (equalCellPoint (-1) 1 128 (i + 1)) :=
  canonicalBumpPacketTransportCell
    (canonicalBumpTaylorCellAt
    canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
    (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
    canonicalBumpPacket_radius_nonneg
    canonicalBumpCorrection1MinusReal_bound canonicalBumpPacket_imaginary_bound
    (canonicalBumpPacket_kernel_exp i)
    (canonicalBumpCorrection1Minus_kernel_trig i)
    (canonicalBumpPacket_bump_lower i)
    (canonicalBumpPacket_bump_upper i)
    (canonicalBumpPacket_window i))
    (canonicalBumpPacket_left_geometry_real i)
    (canonicalBumpPacket_right_geometry_real i)

noncomputable def canonicalBumpCorrection1MinusPacket :
    EqualCellComplexTaylorCertificate
      (computedTransformRawIntegrand canonicalSmoothBump
        ((canonicalBumpCorrection1MinusReal : ℝ) +
          (canonicalBumpPacketImaginaryPart : ℝ) * Complex.I))
      (-1) 1 128 12 where
  cells_pos := by norm_num
  ordered := by norm_num
  integrable := fun _ =>
    (canonicalBumpRawIntegrand_contDiff _).continuous.intervalIntegrable _ _
  cell := canonicalBumpCorrection1MinusCell

def canonicalBumpCorrection1MinusRawCenterQ : ℚ × ℚ :=
  (∑ i : Fin 128, (canonicalBumpCellCenterQ
      canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).re.center,
    ∑ i : Fin 128, (canonicalBumpCellCenterQ
      canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).im.center)

def canonicalBumpCorrection1MinusRawErrorQ : ℚ :=
  ∑ i : Fin 128, canonicalBumpCellErrorQ
    canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
    (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius

theorem canonicalBumpCorrection1MinusCell_center (i : Fin 128) :
    (canonicalBumpCorrection1MinusCell i).center =
      ((canonicalBumpCellCenterQ
        canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).re.center : ℝ) +
      ((canonicalBumpCellCenterQ
        canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius).im.center : ℝ) *
        Complex.I := by
  rw [canonicalBumpCorrection1MinusCell, canonicalBumpPacketTransportCell_center]
  exact canonicalBumpTaylorCellAt_center
      canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
      canonicalBumpPacket_radius_nonneg
      canonicalBumpCorrection1MinusReal_bound canonicalBumpPacket_imaginary_bound
      (canonicalBumpPacket_kernel_exp i)
      (canonicalBumpCorrection1Minus_kernel_trig i)
      (canonicalBumpPacket_bump_lower i)
      (canonicalBumpPacket_bump_upper i)
      (canonicalBumpPacket_window i)

theorem canonicalBumpCorrection1MinusCell_error (i : Fin 128) :
    (canonicalBumpCorrection1MinusCell i).error =
      (canonicalBumpCellErrorQ
        canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
        (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius : ℝ) := by
  rw [canonicalBumpCorrection1MinusCell, canonicalBumpPacketTransportCell_error]
  exact canonicalBumpTaylorCellAt_error
      canonicalBumpCorrection1MinusReal canonicalBumpPacketImaginaryPart
      (canonicalBumpPacketMidpoint i) canonicalBumpPacketRadius
      canonicalBumpPacket_radius_nonneg
      canonicalBumpCorrection1MinusReal_bound canonicalBumpPacket_imaginary_bound
      (canonicalBumpPacket_kernel_exp i)
      (canonicalBumpCorrection1Minus_kernel_trig i)
      (canonicalBumpPacket_bump_lower i)
      (canonicalBumpPacket_bump_upper i)
      (canonicalBumpPacket_window i)

theorem canonicalBumpCorrection1MinusPacket_center :
    canonicalBumpCorrection1MinusPacket.center =
      ((canonicalBumpCorrection1MinusRawCenterQ.1 : ℚ) : ℝ) +
        ((canonicalBumpCorrection1MinusRawCenterQ.2 : ℚ) : ℝ) * Complex.I := by
  rw [EqualCellComplexTaylorCertificate.center]
  unfold canonicalBumpCorrection1MinusPacket canonicalBumpCorrection1MinusRawCenterQ
  simp_rw [show ∀ i : Fin 128,
    (canonicalBumpCorrection1MinusCell i).center = _ from canonicalBumpCorrection1MinusCell_center]
  push_cast
  rw [Finset.sum_add_distrib, Finset.sum_mul]

theorem canonicalBumpCorrection1MinusPacket_error :
    canonicalBumpCorrection1MinusPacket.error = (canonicalBumpCorrection1MinusRawErrorQ : ℝ) := by
  rw [EqualCellComplexTaylorCertificate.error]
  unfold canonicalBumpCorrection1MinusPacket canonicalBumpCorrection1MinusRawErrorQ
  simp_rw [canonicalBumpCorrection1MinusCell_error]
  push_cast
  rfl

end

end RiemannVenue.Venue
