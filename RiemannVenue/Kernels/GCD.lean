import RiemannVenue.Basic

/-!
# Finite GCD kernel

Target object: `K(m,n) = gcd(m,n) / sqrt(m*n)` on finite truncations.
-/

namespace RiemannVenue.Kernels

-- TODO: define finite GCD kernel over positive natural indices.

end RiemannVenue.Kernels
