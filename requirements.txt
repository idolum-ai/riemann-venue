# Pinned exactly: the figure drift gate in CI (git diff --exit-code figures/)
# requires byte-reproducible matplotlib output, which requires identical
# library versions. Update pins and regenerate figures together.
numpy==2.4.6
scipy==1.17.1
matplotlib==3.11.0
sympy==1.14.0
nbformat==5.10.4
nbclient==0.11.0
ipykernel==7.3.0
